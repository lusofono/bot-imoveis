services:
  bot-mail:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:${BOT_MAIL_PORT:-8000}:8000"
    volumes:
      - ./apalace/rent:/data
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
